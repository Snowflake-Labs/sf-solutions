default: true
MD013: false # line-length — SKILL.md trigger descriptions are intentionally long
MD041: false # first-line H1 — frontmatter files don't start with an H1
MD033: false # inline HTML — README uses <details> blocks
